<nav>
    <ul class="pagination pagination-lg" style="margin-left:44%;">
    <% 1.upto(@total_pages.to_i) do |n| %>
        <li class="pag-<%= n %>"><%= link_to n, { controller: controller, action: action, page: n } %></li>
    <% end %>
    </ul>
</nav>